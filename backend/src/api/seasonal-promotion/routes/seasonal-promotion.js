'use strict';

/**
 * seasonal-promotion router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::seasonal-promotion.seasonal-promotion');
