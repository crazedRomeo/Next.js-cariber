'use strict';

/**
 * seasonal-promotion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::seasonal-promotion.seasonal-promotion');
